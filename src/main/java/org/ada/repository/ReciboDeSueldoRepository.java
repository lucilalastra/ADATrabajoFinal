package org.ada.repository;

import org.ada.entity.ReciboDeSueldo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReciboDeSueldoRepository extends JpaRepository<ReciboDeSueldo, Integer> {
}
